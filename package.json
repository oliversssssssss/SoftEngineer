{
  "name": "studentsys-root",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n NODE,FACE -c blue,green \"npm:dev:node\" \"npm:dev:face\"",
    "dev:node": "cd backend && npm run dev",
    "dev:face": "cd face_api && conda run -n vision_all python -m uvicorn main:app --host 127.0.0.1 --port 8001 --workers 1"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "cross-env": "^7.0.3"
  }
}
